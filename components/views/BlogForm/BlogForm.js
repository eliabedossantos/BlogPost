import React from 'react';
import { TopContainer, H1 } from './BlogForm.styled';
export default props => {
    return (
        <TopContainer>
            <H1>Blog Form</H1>
        </TopContainer>
    );
}